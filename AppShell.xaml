<Shell
    x:Class="MauiAuthApp.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:MauiAuthApp.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="MauiAuthApp">

    <!-- Uygulama açıldığında ilk gösterilecek olan sayfa. -->
    <ShellContent
        Title="Giriş"
        ContentTemplate="{DataTemplate local:LoginPage}"
        Route="LoginPage" />

</Shell>